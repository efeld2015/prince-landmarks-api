document.querySelector('button').addEventListener('click', apiRequest)
// async function
async function apiRequest(){
    const landmarkName = document.querySelector('input').value
    try {
        const response = await fetch()
    } catch { // API url goes here -- insert that once you have deployed API to Railway

    }
}